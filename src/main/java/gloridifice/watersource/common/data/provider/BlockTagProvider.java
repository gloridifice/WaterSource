/*
package gloridifice.watersource.common.data.provider;

import gloridifice.watersource.common.data.tag.ModTags;
import net.minecraft.data.BlockTagsProvider;
import net.minecraft.data.DataGenerator;
import net.minecraftforge.common.data.ExistingFileHelper;
import net.minecraftforge.common.data.ForgeBlockTagsProvider;

public class BlockTagProvider extends ForgeBlockTagsProvider {
    public BlockTagProvider(DataGenerator gen, ExistingFileHelper existingFileHelper) {
        super(gen, existingFileHelper);
    }

    @Override
    public void registerTags() {
        getOrCreateBuilder(ModTags.Block.COCONUT_LOGS);
        getOrCreateBuilder(ModTags.Block.STRAINERS);
        getOrCreateBuilder(ModTags.Block.PURIFICATION_STRAINERS);
        getOrCreateBuilder(ModTags.Block.SOUL_STRAINERS);
        getOrCreateBuilder(ModTags.Block.COCONUTS_SOIL);
    }
}
*/
